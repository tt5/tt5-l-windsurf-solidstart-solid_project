podman build . -t solid-project
podman run -d -p 3000:3000 --replace --name solid-project localhost/solid-project
podman kube play --replace solid-project.yml
podman kube down solid-project.yml

npx prism mock -d api.yml

node scripts/simple-create-db.js 

uvx datasette serve data/app.db --metadata data/app.yml